// Starter template for Notification Service

const sendEmailNotification = async (toEmail, subject, content) => {
  // TODO: Add your notification provider logic here
  console.log(`[Notification Placeholder] To: ${toEmail} | Subject: ${subject}`);
  return true;
};

module.exports = {
  sendEmailNotification,
};
